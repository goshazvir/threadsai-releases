# ThreadsAI — релизы клиента

Здесь публикуются подписанные архивы ThreadPilot.
Собираются автоматически из приватного репозитория и подписываются minisign.

Установка — одной командой, архивы отсюда скачивает установщик:

    bash <(curl -fsSL https://threadpilot-self.vercel.app/install.sh)

Проверить подпись вручную:

    minisign -Vm <файл>.zip -x <файл>.zip.minisig -P RWTbdwMhi7xU79V3VYUj5uFbS1GdTaEyl7BvEkt2OvfN6Jj/LaPrvp+B
